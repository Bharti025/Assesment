<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Traffic Analytics Dashboard</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-4">
        <h2 class="text-center">Traffic Analytics Dashboard</h2>
        <p class="text-center"><strong>Target Date:</strong> <%= TARGET_DATE %></p>
        
        <div class="card text-bg-light p-3 mb-4">
            <h4>Total Requests: <%= totalRequests %></h4>
        </div>

        <div class="row">
            <div class="col-md-6">
                <h4>📌 IPs contributing to 85% of traffic</h4>
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th class="table-info">IP Address</th>
                            <th class="table-info">Occurences</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% outputIP.forEach(ip => { %>
                            <tr>
                                <td class="table-light"><%= ip[0] %></td>
                                <td><%= ip[1]%></td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>

            <div class="col-md-6">
                <h4>📌 Hours contributing to 70% of traffic</h4>
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th class="table-info">Hour</th>
                            <th class="table-info">Visitors</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% filterHour.forEach(hour => { %>
                            <tr>
                                <td class="table-light"><%= hour[0] %>:00</td>
                                <td><%= hour[1]%></td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</body>
</html>
